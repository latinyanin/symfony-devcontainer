#!/bin/bash

# Check if the launch.json exists
# if ! [ -f .vscode/launch.json ]; then
#   printf "\nCopy vscode launch.json file.. \n"
#   mkdir -p .vscode
#   cp /code/.devcontainer/launch.json /code/.vscode/launch.json
# fi

# exec symfony server:start --no-tls --port=8000 --allow-http --no-interaction > /dev/null 2>&1 &
